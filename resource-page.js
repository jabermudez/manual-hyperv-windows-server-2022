(async function(){const mount=document.getElementById('resourceMount');try{const response=await fetch('index.html');const documentSource=new DOMParser().parseFromString(await response.text(),'text/html');const sources=documentSource.getElementById('fuentes').cloneNode(true);sources.removeAttribute('id');mount.replaceChildren(sources);}catch{mount.innerHTML='<p>No fue posible cargar las fuentes. Regrese a la portada e inténtelo de nuevo.</p>';}})();
